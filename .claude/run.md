Run commands:


# Platform (dashboard)
cd c:\sniplyv2\platform && npm run dev    # → port 3001

# Website (marketing)
cd c:\sniplyv2\website && npm run dev     # → port 3000

# Backend
cd c:\sniplyv2\backend\Lynx.Api && dotnet run --launch-profile http  # → port 5055